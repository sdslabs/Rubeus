language: cpp
sudo: required

compiler:
    - gcc
    
os:
    - linux

before_script:
    - sudo apt install mesa-common-dev libxinerama-dev libxcursor-dev cmake-qt-gui libglew-dev
    - sudo apt-get install libglwf3-dev xorg-dev libx11-dev libxrandr-dev libxi-dev libudev-dev libopenal-dev libdevil1c2 libdevil-dev libsfml-dev
    - cmake ./
    
script:
    - make
